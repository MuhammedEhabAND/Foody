<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Foody" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimaryLight</item>
        <item name="colorOnPrimary">@color/white</item>

<!--        <item name="android:windowBackground">@color/backgroundLight2</item>-->
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorSecondaryLight</item>
        <item name="colorTertiary">@color/colorTeratiryLight</item>
        <item name="colorError">@color/colorErrorLight</item>
        <item name="background">@color/LightBackground</item>
        <item name="colorOutline">@color/outlineLight</item>

        <item name="android:textColor">@color/textLight</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="materialCardViewStyle">@style/CardViewStyleLight</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/status</item>
        <!-- Customize your theme here. -->
    </style>

    <color name="textLight">#282828</color>
</resources>